name = "system-online-web"
compatibility_date = "2024-09-23"
workers_dev = true

# Custom domain for the POS frontend
routes = [
  { pattern = "pos.engaz.tech", custom_domain = true },
]

[assets]
directory = "../dist"
not_found_handling = "single-page-application"
