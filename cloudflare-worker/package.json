{
  "name": "brewmaster-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "node --experimental-strip-types test/auth.test.mts && node --experimental-strip-types test/session.integration.test.mts && node --experimental-strip-types test/seed-bootstrap.integration.test.mts && node --experimental-strip-types test/permissions.test.mts && node --experimental-strip-types test/csrf.test.mts && node --experimental-strip-types test/upsert-freshness.test.mts",
    "seed:creds": "node ../scripts/seed-manager-credential.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.1"
  }
}
